bin/
obj/
artifacts/
publish/
dist/
*.user
.vs/
appsettings.local.json
src/AiShot/appsettings.json
*.suo

*.nupkg
web/node_modules/
web/dist/

# Artefatos locais de planejamento (o ROADMAP.md versionado é a fonte pública)
.milestones/
*.local.md

# Configuração e skills locais do Claude Code (ferramental de quem desenvolve,
# não do projeto). O processo de release em si está documentado no RELEASING.md.
.claude/
.agents/

# Saída dos testes (relatórios .trx, cobertura)
artefatos/
TestResults/

# Extensão de navegador — mora em repositório próprio (Plugin-AIShot), não neste.
Plugin AIShot/

# Gerados pelo fluxo de release a partir dos .template correspondentes —
# a versão e o checksum só existem depois que o instalador é compilado.
chocolatey/aishot.nuspec
chocolatey/tools/chocolateyinstall.ps1
